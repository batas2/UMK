#include<stdio.h>
#define suma(a, b) (a + b)
#define for for(;;)
#define if(a, b) ((a > b) ? (a) : (b))
#define drukuj printf("costam");

int main(){
	printf("Suma %d\n", suma(5, 6));
//	for;
if(5, 6);
drukuj;
}
